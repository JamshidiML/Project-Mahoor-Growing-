name: Build score reports

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "07-data/**/*.json"
      - "07-scoring/**"
      - "tools/score_cycle.py"
      - "tools/aggregate_period.py"
      - "tests/test_score_cycle.py"
      - ".github/workflows/build-score-reports.yml"
  push:
    paths:
      - "07-data/**/*.json"
      - "07-scoring/**"
      - "tools/score_cycle.py"
      - "tools/aggregate_period.py"
      - "tests/test_score_cycle.py"
      - ".github/workflows/build-score-reports.yml"

permissions:
  contents: write

jobs:
  score:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run unit tests
        run: python -m unittest discover -s tests -p "test_*.py" -v

      - name: Validate and build weekly reports
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t files < <(find 07-data/weekly -type f -name '*.json' 2>/dev/null | sort)
          if [ "${#files[@]}" -eq 0 ]; then
            echo "No weekly JSON files found."
            exit 0
          fi
          for file in "${files[@]}"; do
            python tools/score_cycle.py "$file"
          done
          python tools/aggregate_period.py

      - name: Commit generated reports
        if: github.event_name == 'push'
        shell: bash
        run: |
          if git diff --quiet -- 08-reports; then
            echo "No report changes."
            exit 0
          fi
          git config user.name "mahoor-report-bot"
          git config user.email "mahoor-report-bot@users.noreply.github.com"
          git add 08-reports
          git commit -m "chore: update family operating reports"
          git push
